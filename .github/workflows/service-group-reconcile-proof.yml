name: Service group reconciliation
on:
  push:
    branches: [tmp/service-group-reconcile]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny
      - name: Format
        run: cargo fmt --all -- --check
      - name: Focused group reconciliation proof
        run: cargo test -p archon-node --test service_groups
      - name: Node tests
        run: cargo test -p archon-node
      - name: Strict Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Workspace tests
        run: cargo test --workspace
      - name: Simulator
        run: cargo run -p archon-sim
      - name: Supply chain
        run: cargo deny check advisories bans licenses sources
      - name: Publish validated source tree
        shell: bash
        run: |
          set -euxo pipefail
          rm -f .github/workflows/service-group-reconcile-proof.yml
          git add -A
          git config user.name Nick
          git config user.email nijaru7@gmail.com
          git commit -m 'feat(node): reconcile desired service groups with fixed cardinality'
          git push origin HEAD:tmp/service-group-reconcile
