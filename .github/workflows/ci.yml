name: CI

on:
  push:
    branches: [tmp/claimability-upgrade-reconcile]

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/claimability-upgrade-reconcile
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny
      - name: Apply upgrade reconciliation patch
        run: |
          python3 .github/scripts/reconcile_claim_contracts.py
          cargo fmt --all
      - name: Targeted reconciliation tests
        run: |
          cargo test -p archon-node --test claimability
          cargo test -p archon-control --test persist
          cargo test -p archon-node --test recovery
          cargo test -p archon-node --test devices
      - name: Workspace gates
        run: |
          cargo fmt --all -- --check
          cargo clippy --workspace --all-targets -- -D warnings
          cargo test --workspace
          cargo run -p archon-sim
          cargo deny check advisories bans licenses sources
      - name: Commit verified source
        run: |
          git checkout 6a63008b57fb5ecb22d34f27a97e3953409b526b -- .github/workflows/ci.yml
          rm .github/scripts/reconcile_claim_contracts.py
          git add -A
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "fix(node): reconcile claim contracts on upgrade"
          git push origin HEAD:tmp/claimability-upgrade-reconcile
