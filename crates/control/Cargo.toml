[package]
name = "fleet-control"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[dependencies]
fleet-kernel = { path = "../kernel", features = ["serde"] }
fleet-node = { path = "../node" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
