[package]
name = "archon-cli"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[dependencies]
archon-kernel = { path = "../kernel" }
archon-node = { path = "../node" }
archon-control = { path = "../control" }
serde_json = "1"

[[bin]]
name = "archon"
path = "src/main.rs"
