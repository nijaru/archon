[package]
name = "fleet-kernel"
version = "0.0.0"
edition.workspace = true
publish.workspace = true
description = "Fleet resource graph, lease, and binding transition function"

[dependencies]
serde = { version = "1", optional = true, features = ["derive"] }

[features]
serde = ["dep:serde"]
