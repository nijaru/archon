[package]
name = "archon-sim"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Deterministic Archon kernel simulator"

[dependencies]
archon-kernel = { path = "../kernel" }
